<Window x:Class="WinIsland.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinIsland 设置" 
        Icon="ico.ico"
        Height="600" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        MouseDown="Window_MouseDown">
    
    <Window.Resources>
        <!-- 调色板 -->
        <Color x:Key="WindowBgColor">#1E1E1E</Color>
        <Color x:Key="CardBgColor">#252526</Color> 
        <Color x:Key="AccentColor">#007ACC</Color>
        <Color x:Key="TextColor">#E0E0E0</Color>
        <Color x:Key="SecondaryTextColor">#858585</Color>
        <Color x:Key="BorderColor">#333333</Color>

        <SolidColorBrush x:Key="WindowBgBrush" Color="{StaticResource WindowBgColor}"/>
        <SolidColorBrush x:Key="CardBgBrush" Color="{StaticResource CardBgColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource SecondaryTextColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>

        <!-- DatePicker Styling -->
        <Style TargetType="DatePickerTextBox">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePickerTextBox">
                        <TextBox x:Name="PART_TextBox" 
                                 Text="{Binding Path=SelectedDate, StringFormat='yyyy/MM/dd', RelativeSource={RelativeSource AncestorType={x:Type DatePicker}}}" 
                                 Background="{TemplateBinding Background}" 
                                 Foreground="{TemplateBinding Foreground}" 
                                 BorderThickness="0"
                                 Padding="5,2"
                                 VerticalAlignment="Center"
                                 IsReadOnly="True"
                                 Cursor="Hand"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="{StaticResource CardBgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- 文本样式 -->
        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Margin" Value="0,20,0,10"/>
        </Style>

        <!-- 现代化按钮样式 -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource CardBgBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2D2D30"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0063A5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 现代化 CheckBox 样式 (切换开关外观) -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0" VerticalAlignment="Center"/>
                            <Border Grid.Column="1" Width="40" Height="20" CornerRadius="10" 
                                    Background="#333333" BorderBrush="#555555" BorderThickness="1">
                                <Ellipse x:Name="Knob" Width="16" Height="16" Fill="#AAAAAA" HorizontalAlignment="Left" Margin="2,0,0,0"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Knob" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Knob" Property="Fill" Value="White"/>
                                <Setter TargetName="Knob" Property="Margin" Value="0,0,2,0"/>
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="{StaticResource AccentColor}"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 简单 TextBox 样式 -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource CardBgBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Border Background="{StaticResource WindowBgBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="30">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.4"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- 标题 & 关闭 -->
                <RowDefinition Height="*"/>    <!-- 内容 -->
                <RowDefinition Height="Auto"/> <!-- 底部 -->
            </Grid.RowDefinitions>

            <!-- 头部 -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="设置" Style="{StaticResource TitleTextStyle}"/>
                <Button Grid.Column="1" Content="✕" Click="Cancel_Click" Background="Transparent" BorderThickness="0" Foreground="{StaticResource SecondaryTextBrush}" FontSize="16" Padding="5" VerticalAlignment="Top" Margin="0,-10,-10,0"/>
            </Grid>

            <!-- 可滚动内容区域 -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" Background="Transparent" Margin="0,0,-15,0" Padding="0,0,15,0">
                <StackPanel>
                    
                    <!-- 通知 -->
                    <TextBlock Text="通知" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,10"/>
                    <StackPanel>
                        <CheckBox x:Name="ChkBluetoothNotification" Content="蓝牙设备"/>
                        <CheckBox x:Name="ChkUsbNotification" Content="USB 设备"/>
                        <CheckBox x:Name="ChkMessageNotification" Content="微信 / QQ 消息"/>
                    </StackPanel>

                    <!-- 媒体 -->
                    <TextBlock Text="媒体" Style="{StaticResource SectionHeaderStyle}"/>
                    <StackPanel>
                        <CheckBox x:Name="ChkShowMediaPlayer" Content="显示媒体播放器"/>
                        <CheckBox x:Name="ChkShowVisualizer" Content="显示音频可视化"/>
                    </StackPanel>

                    <!-- 系统 -->
                    <TextBlock Text="系统" Style="{StaticResource SectionHeaderStyle}"/>
                    <CheckBox x:Name="ChkStartWithWindows" Content="开机自启动"/>

                    <!-- 健康与专注 -->
                    <TextBlock Text="健康与专注" Style="{StaticResource SectionHeaderStyle}"/>
                    
                    <CheckBox x:Name="ChkDrinkWater" Content="喝水提醒" Checked="ChkDrinkWater_Checked" Unchecked="ChkDrinkWater_Unchecked"/>
                    
                    <StackPanel x:Name="PanelDrinkWaterSettings" Margin="0,10,0,0" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <RadioButton x:Name="RbModeInterval" Content="间隔提醒" GroupName="DrinkMode" IsChecked="True" Foreground="{StaticResource TextBrush}" Margin="0,0,20,0" Checked="RbMode_Checked"/>
                            <RadioButton x:Name="RbModeCustom" Content="自定义时间" GroupName="DrinkMode" Foreground="{StaticResource TextBrush}" Checked="RbMode_Checked"/>
                        </StackPanel>

                        <!-- 间隔设置 -->
                        <Grid x:Name="PanelModeInterval">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="间隔 (分钟)" Foreground="{StaticResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtDrinkWaterInterval" Text="30" FontFamily="Consolas"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Width="180">
                                <TextBlock Text="活动时间段" Foreground="{StaticResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" x:Name="TxtDrinkStartTime" Text="09:00" FontFamily="Consolas" MaxLength="5"/>
                                    <TextBlock Grid.Column="1" Text="-" Foreground="{StaticResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="5,0"/>
                                    <TextBox Grid.Column="2" x:Name="TxtDrinkEndTime" Text="22:00" FontFamily="Consolas" MaxLength="5"/>
                                </Grid>
                            </StackPanel>
                        </Grid>

                        <!-- 自定义模式设置 -->
                        <StackPanel x:Name="PanelModeCustom" Visibility="Collapsed">
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TxtCustomDrinkTime" Tag="HH:MM" MaxLength="5" FontFamily="Consolas" Margin="0,0,10,0"/>
                                <Button Grid.Column="1" Content="添加时间" Click="BtnAddCustomDrinkTime_Click"/>
                            </Grid>
                            <ItemsControl x:Name="ListCustomDrinkTimes">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource CardBgBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,5" Padding="10,5">
                                            <Grid>
                                                <TextBlock Text="{Binding}" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
                                                <Button Content="×" HorizontalAlignment="Right" Click="BtnDeleteCustomDrinkTime_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Foreground="#FF5555" Padding="5,0"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </StackPanel>

                    <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,15"/>

                    <CheckBox x:Name="ChkTodo" Content="待办事项提醒" Checked="ChkTodo_Checked" Unchecked="ChkTodo_Unchecked"/>
                    
                    <StackPanel x:Name="PanelTodo" Margin="0,10,0,0" Visibility="Collapsed">
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <DatePicker x:Name="DpTodoDate" Grid.Column="0" Margin="0,0,5,0" Foreground="Black"/>
                            <TextBox x:Name="TxtTodoTime" Grid.Column="1" Text="12:00" Margin="0,0,5,0" FontFamily="Consolas"/>
                            <TextBox x:Name="TxtTodoContent" Grid.Column="2" Margin="0,0,5,0"/>
                            <Button Grid.Column="3" Content="+" Click="BtnAddTodo_Click" Width="30"/>
                        </Grid>
                        
                        <ItemsControl x:Name="ListTodo">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource CardBgBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,5" Padding="10,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding ReminderTime, StringFormat='MM/dd HH:mm'}" Foreground="{StaticResource AccentBrush}" FontSize="12" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Content}" Foreground="{StaticResource TextBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                            <Button Grid.Column="2" Content="×" Click="BtnDeleteTodo_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Foreground="#FF5555" Padding="5,0"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>

            <!-- 底部 -->
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="取消" Click="Cancel_Click" Margin="0,0,5,0"/>
                <Button Grid.Column="1" Content="保存更改" Click="Save_Click" Style="{StaticResource PrimaryButton}" Margin="5,0,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>
